<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="oval">
            <size android:width="16dp" android:height="16dp" /> <!-- Adjust the size as needed -->
            <solid android:color="@color/winit_blue" />
        </shape>
    </item>
    <item android:state_checked="false">
        <shape android:shape="oval">
            <size android:width="16dp" android:height="16dp" /> <!-- Adjust the size as needed -->
            <stroke
                android:width="2dp"
                android:color="@color/winit_blue" />
        </shape>
    </item>
</selector>
